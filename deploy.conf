# deploy.conf

# Absolute path to your local project repository
PROJECT_DIR="/var/www/webpage"

# Absolute path to your web root where Nginx serves files
WEB_ROOT="/var/www/webpage-test"

# Docker image name for temporary build container
DOCKER_IMAGE_NAME="webpage-test"

# Git branch to pull for deployment
GIT_BRANCH="main"
